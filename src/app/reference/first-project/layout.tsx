import type { Metadata } from "next";
export const metadata: Metadata = { title: "First Project Calculator - Sew", description: "Free online first project calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
