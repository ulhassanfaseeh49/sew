import type { Metadata } from "next";
export const metadata: Metadata = { title: "Technique Library Calculator - Sew", description: "Free online technique library calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
