import type { Metadata } from "next";
export const metadata: Metadata = { title: "Abbreviations Calculator - Sew", description: "Free online abbreviations calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
