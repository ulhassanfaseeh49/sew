import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Prep Calculator - Sew", description: "Free online fabric prep calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
