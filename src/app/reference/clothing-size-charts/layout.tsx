import type { Metadata } from "next";
export const metadata: Metadata = { title: "Clothing Size Charts Calculator - Sew", description: "Free online clothing size charts calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
