import type { Metadata } from "next";
export const metadata: Metadata = { title: "Table Sizes Calculator - Sew", description: "Free online table sizes calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
