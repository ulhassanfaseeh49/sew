import type { Metadata } from "next";
export const metadata: Metadata = { title: "Conversion Quick Ref Calculator - Sew", description: "Free online conversion quick ref calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
