import type { Metadata } from "next";
export const metadata: Metadata = { title: "Stitch Database Calculator - Sew", description: "Free online stitch database calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
