import type { Metadata } from "next";
export const metadata: Metadata = { title: "Project Difficulty Calculator - Sew", description: "Free online project difficulty calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
