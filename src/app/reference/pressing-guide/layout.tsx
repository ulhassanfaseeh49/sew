import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pressing Guide Calculator - Sew", description: "Free online pressing guide calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
