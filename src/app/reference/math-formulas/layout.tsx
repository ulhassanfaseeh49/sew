import type { Metadata } from "next";
export const metadata: Metadata = { title: "Math Formulas Calculator - Sew", description: "Free online math formulas calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
