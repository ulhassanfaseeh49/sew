import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pillow Sizes Calculator - Sew", description: "Free online pillow sizes calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
