import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fitting Flowchart Calculator - Sew", description: "Free online fitting flowchart calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
