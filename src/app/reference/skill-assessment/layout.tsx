import type { Metadata } from "next";
export const metadata: Metadata = { title: "Skill Assessment Calculator - Sew", description: "Free online skill assessment calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
