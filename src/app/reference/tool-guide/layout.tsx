import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tool Guide Calculator - Sew", description: "Free online tool guide calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
