import type { Metadata } from "next";
export const metadata: Metadata = { title: "Window Sizes Calculator - Sew", description: "Free online window sizes calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
