import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pattern Symbols Calculator - Sew", description: "Free online pattern symbols calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
