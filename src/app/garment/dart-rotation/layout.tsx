import type { Metadata } from "next";
export const metadata: Metadata = { title: "Dart Rotation Calculator - Sew", description: "Free online dart rotation calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
