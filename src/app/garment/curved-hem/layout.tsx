import type { Metadata } from "next";
export const metadata: Metadata = { title: "Curved Hem Calculator - Sew", description: "Free online curved hem calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
