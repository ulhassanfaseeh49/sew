import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pleat Calculator Calculator - Sew", description: "Free online pleat calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
