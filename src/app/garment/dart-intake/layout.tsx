import type { Metadata } from "next";
export const metadata: Metadata = { title: "Dart Intake Calculator - Sew", description: "Free online dart intake calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
