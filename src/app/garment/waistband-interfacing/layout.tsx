import type { Metadata } from "next";
export const metadata: Metadata = { title: "Waistband Interfacing Calculator - Sew", description: "Free online waistband interfacing calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
