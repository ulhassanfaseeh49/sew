import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sleeve Cap Ease Calculator - Sew", description: "Free online sleeve cap ease calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
