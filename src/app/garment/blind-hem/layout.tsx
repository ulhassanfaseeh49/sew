import type { Metadata } from "next";
export const metadata: Metadata = { title: "Blind Hem Calculator - Sew", description: "Free online blind hem calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
