import type { Metadata } from "next";
export const metadata: Metadata = { title: "Hem Allowance Calculator - Sew", description: "Free online hem allowance calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
