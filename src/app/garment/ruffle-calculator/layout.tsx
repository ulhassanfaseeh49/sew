import type { Metadata } from "next";
export const metadata: Metadata = { title: "Ruffle Calculator Calculator - Sew", description: "Free online ruffle calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
