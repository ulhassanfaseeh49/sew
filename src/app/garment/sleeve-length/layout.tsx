import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sleeve Length Calculator - Sew", description: "Free online sleeve length calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
