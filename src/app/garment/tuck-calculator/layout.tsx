import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tuck Calculator Calculator - Sew", description: "Free online tuck calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
