import type { Metadata } from "next";
export const metadata: Metadata = { title: "Dart Calculator Calculator - Sew", description: "Free online dart calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
