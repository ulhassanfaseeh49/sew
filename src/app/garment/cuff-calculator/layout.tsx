import type { Metadata } from "next";
export const metadata: Metadata = { title: "Cuff Calculator Calculator - Sew", description: "Free online cuff calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
