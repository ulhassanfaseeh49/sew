import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fly Zipper Calculator - Sew", description: "Free online fly zipper calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
