import type { Metadata } from "next";
export const metadata: Metadata = { title: "Grain Line Guide Calculator - Sew", description: "Free online grain line guide calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
