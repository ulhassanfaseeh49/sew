import type { Metadata } from "next";
export const metadata: Metadata = { title: "Placket Calculator Calculator - Sew", description: "Free online placket calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
