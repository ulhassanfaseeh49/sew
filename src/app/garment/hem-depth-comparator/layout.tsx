import type { Metadata } from "next";
export const metadata: Metadata = { title: "Hem Depth Comparator Calculator - Sew", description: "Free online hem depth comparator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
