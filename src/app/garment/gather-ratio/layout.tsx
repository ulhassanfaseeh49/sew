import type { Metadata } from "next";
export const metadata: Metadata = { title: "Gather Ratio Calculator - Sew", description: "Free online gather ratio calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
