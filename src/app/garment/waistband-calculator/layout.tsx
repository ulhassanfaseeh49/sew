import type { Metadata } from "next";
export const metadata: Metadata = { title: "Waistband Calculator Calculator - Sew", description: "Free online waistband calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
