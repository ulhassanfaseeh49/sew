import type { Metadata } from "next";
export const metadata: Metadata = { title: "Dart Equivalents Calculator - Sew", description: "Free online dart equivalents calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
