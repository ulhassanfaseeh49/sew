import type { Metadata } from "next";
export const metadata: Metadata = { title: "Collar Stand Calculator - Sew", description: "Free online collar stand calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
