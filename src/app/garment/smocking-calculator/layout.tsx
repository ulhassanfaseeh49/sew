import type { Metadata } from "next";
export const metadata: Metadata = { title: "Smocking Calculator Calculator - Sew", description: "Free online smocking calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
