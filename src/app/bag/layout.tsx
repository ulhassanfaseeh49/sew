import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bag & Accessory Calculators - Sew", description: "19 free calculators for bags, pouches, hats, headbands, and accessories. Calculate fabric, interfacing, and straps." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
