import type { Metadata } from "next";
export const metadata: Metadata = { title: "Messenger Bag Calculator - Sew", description: "Calculate fabric for messenger and crossbody bags. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
