import type { Metadata } from "next";
export const metadata: Metadata = { title: "Laptop Sleeve Calculator - Sew", description: "Calculate padded fabric for laptop sleeves by device size. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
