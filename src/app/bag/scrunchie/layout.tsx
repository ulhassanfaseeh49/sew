import type { Metadata } from "next";
export const metadata: Metadata = { title: "Scrunchie Fabric Calculator - Sew", description: "Calculate fabric for scrunchies with elastic. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
