import type { Metadata } from "next";
export const metadata: Metadata = { title: "Face Mask Fabric Calculator - Sew", description: "Calculate fabric for face masks with filter pocket. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
