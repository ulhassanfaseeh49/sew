import type { Metadata } from "next";
export const metadata: Metadata = { title: "Zipper Pouch Size Calculator - Sew", description: "Calculate fabric for zipper pouches by size. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
