import type { Metadata } from "next";
export const metadata: Metadata = { title: "Headband Calculator - Sew", description: "Calculate fabric for headbands by width and style. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
