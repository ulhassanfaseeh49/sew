import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bag Interfacing Calculator - Sew", description: "Calculate fusible interfacing for bag construction. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
