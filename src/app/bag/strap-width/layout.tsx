import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bag Strap Width Calculator - Sew", description: "Calculate strap width and fabric for bag straps. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
