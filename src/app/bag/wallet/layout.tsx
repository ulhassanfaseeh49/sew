import type { Metadata } from "next";
export const metadata: Metadata = { title: "Wallet Pattern Size Calculator - Sew", description: "Calculate fabric for fabric wallets and card holders. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
