import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bag Strap Length Calculator - Sew", description: "Calculate strap length by carry style. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
