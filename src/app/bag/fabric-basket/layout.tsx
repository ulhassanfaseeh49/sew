import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Basket Calculator - Sew", description: "Calculate fabric for sewn storage baskets. Free online sewing calculator." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
