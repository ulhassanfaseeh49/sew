import type { Metadata } from "next";
export const metadata: Metadata = { title: "Round Cushion Calculator - Sew", description: "Free online round cushion calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
