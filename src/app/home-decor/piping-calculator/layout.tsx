import type { Metadata } from "next";
export const metadata: Metadata = { title: "Piping Calculator Calculator - Sew", description: "Free online piping calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
