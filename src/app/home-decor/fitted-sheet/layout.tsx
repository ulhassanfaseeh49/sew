import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fitted Sheet Calculator - Sew", description: "Free online fitted sheet calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
