import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tablecloth Calculator Calculator - Sew", description: "Free online tablecloth calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
