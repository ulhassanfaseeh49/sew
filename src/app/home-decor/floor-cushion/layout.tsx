import type { Metadata } from "next";
export const metadata: Metadata = { title: "Floor Cushion Calculator - Sew", description: "Free online floor cushion calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
