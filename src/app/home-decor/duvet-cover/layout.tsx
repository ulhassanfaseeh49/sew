import type { Metadata } from "next";
export const metadata: Metadata = { title: "Duvet Cover Calculator - Sew", description: "Free online duvet cover calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
