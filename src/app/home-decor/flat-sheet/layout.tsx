import type { Metadata } from "next";
export const metadata: Metadata = { title: "Flat Sheet Calculator - Sew", description: "Free online flat sheet calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
