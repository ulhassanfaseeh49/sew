import type { Metadata } from "next";
export const metadata: Metadata = { title: "Table Runner Calculator - Sew", description: "Free online table runner calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
