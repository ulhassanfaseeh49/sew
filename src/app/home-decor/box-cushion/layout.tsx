import type { Metadata } from "next";
export const metadata: Metadata = { title: "Box Cushion Calculator - Sew", description: "Free online box cushion calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
