import type { Metadata } from "next";
export const metadata: Metadata = { title: "Cushion Cover Zipper Calculator - Sew", description: "Free online cushion cover zipper calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
