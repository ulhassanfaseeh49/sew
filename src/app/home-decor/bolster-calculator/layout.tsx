import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bolster Calculator Calculator - Sew", description: "Free online bolster calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
