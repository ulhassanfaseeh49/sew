import type { Metadata } from "next";
export const metadata: Metadata = { title: "Placemat Calculator Calculator - Sew", description: "Free online placemat calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
