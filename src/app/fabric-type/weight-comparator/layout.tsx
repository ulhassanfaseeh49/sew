import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Weight Comparator — Compare Multiple Fabrics", description: "Compare up to 6 fabric weights side by side with drape ratings and project recommendations." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
