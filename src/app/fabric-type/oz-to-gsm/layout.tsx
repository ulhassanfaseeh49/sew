import type { Metadata } from "next";
export const metadata: Metadata = { title: "oz/yd² to GSM Converter — Convert Fabric Weight", description: "Free oz/yd² to GSM converter. Convert fabric weight from ounces per square yard to grams per square meter." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
