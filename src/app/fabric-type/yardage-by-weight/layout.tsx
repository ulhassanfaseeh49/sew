import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Yardage by Weight Calculator — Weight to Yards", description: "Convert fabric weight to yardage. Essential for thrift store and bulk fabric purchases." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
