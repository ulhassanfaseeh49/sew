import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Substitution Tool — Find Alternative Fabrics", description: "Find affordable fabric alternatives with match scores and sewing adjustment notes." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
