import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Hand Guide — Drape, Body & Texture Explained", description: "Interactive fabric hand guide. Understand drape, body, and surface texture to choose the right fabric." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
