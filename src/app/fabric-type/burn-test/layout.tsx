import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Burn Test Guide — Identify Unknown Fabric Fiber", description: "Step-by-step fabric burn test guide. Identify unknown fiber content by burn behavior, ash, and smell." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
