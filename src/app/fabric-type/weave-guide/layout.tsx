import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Weave Type Guide — Plain, Twill, Satin & More", description: "Visual guide to fabric weave structures and knit constructions with sewing tips." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
