import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Care Symbol Database — Laundry Symbol Guide", description: "Searchable guide to all international fabric care symbols with washing, drying, ironing, and bleaching instructions." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
