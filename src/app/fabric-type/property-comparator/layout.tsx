import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Property Comparator — Compare Drape, Stretch & More", description: "Compare up to 5 fabrics across 9 properties including weight, drape, breathability, and sewing ease." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
