import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Type Selection Guide — What Fabric Should I Buy?", description: "Interactive guide recommending best fabric types for your project based on garment type, properties, and budget." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
