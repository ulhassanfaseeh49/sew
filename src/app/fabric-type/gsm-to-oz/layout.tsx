import type { Metadata } from "next";
export const metadata: Metadata = { title: "GSM to oz/yd² Converter — Fabric Weight Conversion Tool", description: "Free GSM to oz/yd² converter. Convert fabric weight from grams per square meter to ounces per square yard with weight classification." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
