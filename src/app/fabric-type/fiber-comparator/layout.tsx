import type { Metadata } from "next";
export const metadata: Metadata = { title: "Natural vs Synthetic Fiber Comparator — Cotton, Polyester & More", description: "Compare natural, synthetic, and semi-synthetic fibers across breathability, durability, and eco-score." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
