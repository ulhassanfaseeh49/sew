import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabric Grain Line Calculator — On-Grain Pattern Check", description: "Check if your pattern piece is on grain and learn about straight grain, cross grain, and bias cutting." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
