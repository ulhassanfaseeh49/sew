import type { Metadata } from "next";
export const metadata: Metadata = { title: "Swaddle Blanket Calculator — Muslin Swaddle Fabric Yardage", description: "Free swaddle blanket calculator. Calculate fabric for muslin, flannel, and jersey swaddle blankets in standard and custom sizes. Bulk production planning included." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
