import type { Metadata } from "next";
export const metadata: Metadata = { title: "Children's Costume Size Calculator — Fabric by Age & Character", description: "Free kids costume calculator. Estimate fabric for children's costumes by character type, age, and complexity. Includes notions lists and accessory planning." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
