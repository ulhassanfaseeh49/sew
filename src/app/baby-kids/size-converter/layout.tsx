import type { Metadata } from "next";
export const metadata: Metadata = { title: "Children's Size Conversion Tool — US, UK, EU & Pattern Sizes", description: "Free children's size converter. Convert between US, UK, EU, and pattern brand sizing for ages 0-16. Includes measurement-to-size mapping and between-sizes guidance.", keywords: ["children size conversion", "kids size chart", "EU children sizes", "US to UK kids size", "pattern size children"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
