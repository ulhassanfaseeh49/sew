import type { Metadata } from "next";
export const metadata: Metadata = { title: "Children's Growth Chart by Age — Height, Chest & Waist Reference", description: "Free children's growth chart. Reference heights, chest, waist, and hip measurements by age from newborn to 16 years for sewing pattern sizing." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
