import type { Metadata } from "next";
export const metadata: Metadata = { title: "Stuffed Animal Size Calculator — Fabric for Plush Toys", description: "Free stuffed animal calculator. Calculate fabric, stuffing, and safety eyes for soft toys and plushies by size." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
