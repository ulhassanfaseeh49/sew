import type { Metadata } from "next";
export const metadata: Metadata = { title: "Crib Sheet Calculator — Fitted Sheet Fabric & Elastic Yardage", description: "Free crib sheet calculator. Calculate fabric and elastic for fitted crib sheets, mini crib, and toddler bed sheets." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
