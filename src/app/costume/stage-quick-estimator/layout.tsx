import type { Metadata } from "next";
export const metadata: Metadata = { title: "Stage Quick Estimator Calculator - Sew", description: "Free online stage quick estimator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
