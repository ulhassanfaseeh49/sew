import type { Metadata } from "next";
export const metadata: Metadata = { title: "Worbla Calculator - Sew", description: "Free online worbla calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
