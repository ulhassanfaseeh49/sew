import type { Metadata } from "next";
export const metadata: Metadata = { title: "Corset Yardage Calculator - Sew", description: "Free online corset yardage calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
