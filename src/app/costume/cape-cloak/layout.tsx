import type { Metadata } from "next";
export const metadata: Metadata = { title: "Cape Cloak Calculator - Sew", description: "Free online cape cloak calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
