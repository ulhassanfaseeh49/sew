import type { Metadata } from "next";
export const metadata: Metadata = { title: "Boning Calculator Calculator - Sew", description: "Free online boning calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
