import type { Metadata } from "next";
export const metadata: Metadata = { title: "Yardage By Type Calculator - Sew", description: "Free online yardage by type calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
