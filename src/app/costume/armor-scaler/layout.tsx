import type { Metadata } from "next";
export const metadata: Metadata = { title: "Armor Scaler Calculator - Sew", description: "Free online armor scaler calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
