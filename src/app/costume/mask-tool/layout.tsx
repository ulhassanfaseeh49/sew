import type { Metadata } from "next";
export const metadata: Metadata = { title: "Mask Tool Calculator - Sew", description: "Free online mask tool calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
