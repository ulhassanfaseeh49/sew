import type { Metadata } from "next";
export const metadata: Metadata = { title: "Hood Calculator Calculator - Sew", description: "Free online hood calculator calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
