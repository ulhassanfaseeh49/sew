import type { Metadata } from "next";
export const metadata: Metadata = { title: "Crinoline Calculator - Sew", description: "Free online crinoline calculator for sewing projects." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
